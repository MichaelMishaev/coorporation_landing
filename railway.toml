[build]
builder = "RAILPACK"

[deploy]
# Apply additive Prisma migrations inside Railway's private network before
# the new app revision replaces the currently healthy deployment.
preDeployCommand = "npx prisma migrate deploy"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
